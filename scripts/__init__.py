"""Aizen Trading utility scripts.

These are CLI helpers and are not part of the agent runtime. They can
be invoked with ``python -m scripts.<name>`` (e.g. ``python -m
scripts.build_dashboard``) or directly with ``python scripts/<name>.py``.
"""
